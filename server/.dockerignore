# Build-context root is "server" (Render default). Exclude build output and
# VCS/secret weight so the Docker context stays small.
backend/target
**/target
**/*.jar
**/*.class
.git
.gitignore
*.md
.idea
*.iml
.env
node_modules
