{
  "name": "server",
  "version": "1.0.0",
  "description": "INT Bank Backend (NestJS) — built via Dockerfile from kafka-backend/",
  "private": true,
  "scripts": {
    "build": "echo 'Build is handled by kafka-backend/'",
    "start": "echo 'Start is handled by kafka-backend/'",
    "test": "echo 'Tests are handled by kafka-backend/'"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
